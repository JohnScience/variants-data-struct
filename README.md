# `variants-data-struct` workspace

That's a Rust workspace that notably contains the `variants-data-struct` crate, which you can find in the [`variants-data-struct`](./variants-data-struct/) folder.
